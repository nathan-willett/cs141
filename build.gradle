plugins {
    id 'java'
}

allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    version = '1.0'

    dependencies {
        //testImplementation 'junit:junit:4.13.2'
        compileOnly 'javax:javaee-api:8.0'
    }

    // Example of configuring Java for all subprojects
    apply plugin: 'java'

    sourceSets {
        main {
            java {
                srcDirs = ['src']
            }
        }
    }
}
